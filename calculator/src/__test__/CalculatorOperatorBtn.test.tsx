import { render, screen } from "@testing-library/react";
import { CalculatorOperatorBtn } from "../components";

describe("<>", () => {
  it("default test ", () => {
    expect(1 + 1).toBe(2);
  });
});
